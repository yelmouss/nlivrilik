'use client';

// Définition des statuts possibles pour une commande
const OrderStatus = {
  PENDING: 'pending',
  CONFIRMED: 'confirmed',
  DELIVERED: 'delivered',
  CANCELLED: 'cancelled'
};

export default OrderStatus;
